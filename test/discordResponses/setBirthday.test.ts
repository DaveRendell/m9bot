

describe("setBirthday", () => {
  xit("does not send a reply if the message does not have a user", () => {

  })
  xit("replies with an error if the message contains an incorrectly formatted date", () => {

  })
  xit("replies with a stack trace if there is an error while setting the users birthday", () => {

  })
  xit("replies with a confirmation message when the user's birthday is set successfully", () => {

  })
})