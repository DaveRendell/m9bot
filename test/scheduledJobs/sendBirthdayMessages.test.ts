

describe("sendBirthdayMessages", () => {
  xit("sends no messages if there are no birthdays matching today's date", () => {

  })
  xit("sends no messages if the main channel cannot be found", () => {

  })
  xit("sends no messages if the main channel is not a text channel", () => {
    
  })
  xit("sends no messages if the user ID does not match a user in the channel", () => {
    
  })
  xit("sends a birthday message if a user has a matching birthday", () => {
    
  })
  xit("sends multiple messages if multiple users have birthdays", () => {
    
  })
})